@page
@model EDTProjectM1.DetailsGroupe

@{
    ViewData["Title"] = "Details";
}

<h1>Détail</h1>

<div>
    <h4>Groupe</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Groupe.NomGroupe)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Groupe.NomGroupe)
        </dd>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Groupe.UE.NomComplet)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Groupe.ID">Modifier</a> |
    <a asp-page="./Index">Retourer à la liste des groupes</a>
</div>
