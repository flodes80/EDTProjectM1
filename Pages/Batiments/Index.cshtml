@page
@model EDTProjectM1.IndexBatiment

@{
    ViewData["Title"] = "Index";
}

<h1>Liste des bâtiments</h1>

<p>
    <a asp-page="Create">Créer nouveau bâtiment</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Batiment[0].NomBatiment)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Batiment) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomBatiment)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Modifier</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Détail</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Supprimer</a>
            </td>
        </tr>
}
    </tbody>
</table>
